package arena;

@:publicFields
class ArenaScreenGuiRes {
    static var timerText(default, never) = "timerText";
    static var timer_fill(default, never) = "timer_fill";

    static var playerScore(default, never) = "playerScore";
    static var oponentScore(default, never) = "oponentScore";
}